from BoardState import BoardState

def test_8139(state, player):
  # Runs our minimax pruning algorithm?
  # Returns next move 
  # Has no relation to the game object/gui, doesn't need to create one

  return 0